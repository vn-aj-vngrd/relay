import { notFound } from "next/navigation";
import { Broadcast, Users } from "@phosphor-icons/react/dist/ssr";
import { Avatar } from "@/components/shared/avatar-stack";
import { profileAvatarUrl } from "@/features/players/avatar";
import { LiveCourt } from "@/features/matches/live-court";
import { getPublicLiveSession } from "@/features/matches/queries";
import { sessionAccentStyle } from "@/features/sessions/accent";
import { RealtimeRefresh } from "@/features/sessions/realtime-refresh";

function name(player: { guestName: string | null }, profile: { name: string } | null) { return profile?.name ?? player.guestName ?? "Guest"; }

export default async function PublicCourtsPage({ params }: { params: Promise<{ slug: string }> }) {
  const slug = (await params).slug;
  const data = await getPublicLiveSession(slug);
  if (!data) notFound();
  const waiting = data.queue.filter(({ queue }) => queue.state === "waiting");
  return <main id="main-content" className="min-h-screen bg-canvas" style={sessionAccentStyle(data.session.accentColor)}><div className="mx-auto max-w-[1040px] bg-surface px-4 py-8 sm:mt-8 sm:rounded-xl sm:border sm:border-line sm:px-8"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm font-semibold text-primary">{data.session.title}</p><h1 className="mt-1 app-title">Courts and scores</h1><p className="mt-2 text-sm text-muted">Live matches and the paddle stack update for everyone with this link.</p></div><RealtimeRefresh sessionId={data.session.id} compact /></div>{data.session.status !== "live" && !data.activeMatches.length ? <section className="mt-10 border-y border-line py-12 text-center"><Broadcast aria-hidden size={25} className="mx-auto text-primary" /><h2 className="mt-4 text-xl font-bold">Live Mode hasn’t started</h2><p className="mt-2 text-sm text-muted">The host will start courts and the paddle stack when everyone arrives.</p></section> : <div className="mt-8 grid gap-8 lg:grid-cols-[1fr_300px]"><section><h2 className="mb-4 text-lg font-bold">Active courts</h2>{data.activeMatches.length ? <div className="grid gap-5">{data.activeMatches.map((match) => { const teamA = match.players.filter(({ matchPlayer }) => matchPlayer.team === "A").map(({ player, profile }) => name(player, profile)).join(" + "); const teamB = match.players.filter(({ matchPlayer }) => matchPlayer.team === "B").map(({ player, profile }) => name(player, profile)).join(" + "); return <LiveCourt key={match.id} sessionId={data.session.id} matchId={match.id} number={match.courtLabel} teams={[teamA, teamB]} scores={[match.teamAScore, match.teamBScore]} version={match.version} canScore={false} />; })}</div> : <div className="border-y border-line py-10"><h3 className="font-bold">No match is active</h3><p className="mt-2 text-sm text-muted">Watch the paddle stack for the next court assignment.</p></div>}</section><aside><div className="flex items-center gap-2"><Users aria-hidden size={18} className="text-primary" /><h2 className="text-lg font-bold">Up next</h2></div>{waiting.length ? <ol className="mt-3 divide-y divide-line border-y border-line">{waiting.map(({ queue, player, profile }, index) => { const playerName = name(player, profile); return <li key={queue.sessionPlayerId} className="flex min-h-14 items-center gap-3"><span className="score w-5 text-sm text-muted">{index + 1}</span><Avatar name={playerName} imageUrl={profileAvatarUrl(profile?.avatarPath)} index={index} size="sm" /><span className="font-medium">{playerName}</span></li>; })}</ol> : <p className="mt-3 border-y border-line py-7 text-sm text-muted">No one is waiting right now.</p>}{data.standings.length ? <section className="mt-9"><h2 className="text-lg font-bold">Session Standings</h2><div className="mt-3 overflow-hidden border-y border-line"><table className="w-full text-sm"><thead className="text-left text-xs text-muted"><tr><th className="py-2">Player</th><th className="py-2 text-right">W</th><th className="py-2 text-right">L</th><th className="py-2 text-right">+/−</th></tr></thead><tbody className="divide-y divide-line">{data.standings.map((row) => <tr key={row.playerId}><td className="py-3 font-medium">{row.name}</td><td className="score py-3 text-right">{row.wins}</td><td className="score py-3 text-right">{row.losses}</td><td className="score py-3 text-right">{row.differential > 0 ? "+" : ""}{row.differential}</td></tr>)}</tbody></table></div></section> : null}</aside></div>}</div></main>;
}
