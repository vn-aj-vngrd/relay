import { MapPin } from "lucide-react";
import { Avatar } from "@/components/shared/avatar-stack";
import { recent } from "@/features/sessions/demo-data";

export default function ProfilePage() { return <div className="mx-auto max-w-3xl"><header className="flex items-center gap-4 border-b border-line pb-7"><Avatar name="Van J." size="lg" /><div className="flex-1"><h1 className="text-2xl font-bold tracking-[-0.03em]">Van</h1><p className="mt-1 flex items-center gap-1 text-sm text-muted"><MapPin size={14} />Mandaluyong · Right-handed</p></div><button className="min-h-11 rounded-[10px] border border-line px-4 text-sm font-semibold">Edit profile</button></header><section className="grid grid-cols-3 border-b border-line py-6 text-center"><div><strong className="score block text-2xl">32</strong><span className="text-sm text-muted">Sessions</span></div><div><strong className="score block text-2xl">118</strong><span className="text-sm text-muted">Matches</span></div><div><strong className="score block text-2xl">64</strong><span className="text-sm text-muted">Wins</span></div></section><section className="py-8"><h2 className="text-lg font-bold">Recent sessions</h2><ul className="mt-3 divide-y divide-line border-y border-line">{recent.map((game) => <li key={game.title} className="flex items-center justify-between py-4"><div><p className="font-semibold">{game.title}</p><p className="mt-1 text-sm text-muted">{game.date} · {game.venue}</p></div><span className="text-sm text-muted">{game.matches} matches</span></li>)}</ul></section></div>; }
