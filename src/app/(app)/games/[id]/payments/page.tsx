import { CircleCheck, CircleDashed, QrCode } from "lucide-react";
import { SessionNav } from "@/components/shared/session-nav";
import { Button } from "@/components/ui/button";
import { session } from "@/features/sessions/demo-data";

export default function PaymentsPage() { return <div><div className="mb-5"><p className="text-sm font-semibold text-primary">{session.title}</p><h1 className="mt-1 text-[28px] font-bold tracking-[-0.035em]">Payments</h1></div><SessionNav id={session.id} active="More" /><div className="grid gap-8 pt-7 lg:grid-cols-[1fr_340px]"><section><div className="flex items-end justify-between border-b border-line pb-5"><div><p className="text-sm text-muted">Court · 8 paying players</p><p className="score mt-1 text-3xl font-bold">₱2,400 <span className="text-base font-medium text-muted">total</span></p></div><p className="score text-lg font-bold text-primary">₱300/person</p></div><ul className="divide-y divide-line">{session.players.map((name, i) => <li key={name} className="flex min-h-16 items-center justify-between"><span className="font-medium">{name}</span><span className={`inline-flex items-center gap-1.5 text-sm font-medium ${i < 5 ? "text-success" : "text-muted"}`}>{i < 5 ? <CircleCheck size={16} /> : <CircleDashed size={16} />}{i < 5 ? "Paid" : "Unpaid"}</span></li>)}</ul></section><aside className="self-start rounded-xl border border-line p-5"><span className="grid h-12 w-12 place-items-center rounded-[10px] bg-surface-strong"><QrCode /></span><h2 className="mt-4 font-bold">Pay Van with GCash</h2><p className="mt-2 text-sm leading-5 text-muted">0917 123 4567 · Van J.</p><p className="score mt-5 text-3xl font-bold">₱300</p><Button className="mt-5 w-full">Mark payment sent</Button><p className="mt-3 text-center text-xs text-muted">Van will confirm when received.</p></aside></div></div>; }
