import { MoreHorizontal, UserPlus } from "lucide-react";
import { Avatar } from "@/components/shared/avatar-stack";
import { SessionNav } from "@/components/shared/session-nav";
import { Button } from "@/components/ui/button";
import { session } from "@/features/sessions/demo-data";

export default function PlayersPage() { return <div><div className="mb-5 flex items-end justify-between"><div><p className="text-sm font-semibold text-primary">{session.title}</p><h1 className="mt-1 text-[28px] font-bold tracking-[-0.035em]">Players</h1></div><Button><UserPlus size={17} />Add player</Button></div><SessionNav id={session.id} active="Players" /><section className="mx-auto max-w-2xl pt-7"><div className="flex items-end justify-between"><div><h2 className="text-lg font-bold">Going</h2><p className="mt-1 text-sm text-muted">{session.players.length} of {session.capacity} spots filled</p></div><span className="score text-2xl font-bold text-primary">{session.capacity - session.players.length} left</span></div><ul className="mt-4 divide-y divide-line border-y border-line">{session.players.map((player, i) => <li key={player} className="flex min-h-16 items-center gap-3 py-2"><Avatar name={player} index={i} size="sm" /><span className="flex-1 font-medium">{player}{i === 0 ? <span className="ml-2 text-xs font-normal text-muted">Host</span> : null}</span><button aria-label={`Options for ${player}`} className="grid h-11 w-11 place-items-center rounded-full hover:bg-surface"><MoreHorizontal size={19} /></button></li>)}</ul><div className="mt-9"><h2 className="text-lg font-bold">Waitlist</h2><p className="mt-2 text-sm text-muted">No one is waiting. New players will be added here when the game is full.</p></div></section></div>; }
