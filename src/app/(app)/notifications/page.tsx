import { CalendarDays, CircleDollarSign, UserPlus } from "lucide-react";

const items = [{ icon: CircleDollarSign, title: "Payment requested", body: "Van requested ₱300 for Saturday Night Pickle.", time: "10m" }, { icon: UserPlus, title: "Mika joined", body: "Saturday Night Pickle now has 8 players.", time: "2h" }, { icon: CalendarDays, title: "Game tomorrow", body: "Tuesday Dink Club starts tomorrow at 8:00 PM.", time: "1d" }];
export default function NotificationsPage() { return <div className="mx-auto max-w-2xl"><h1 className="text-[28px] font-bold tracking-[-0.035em]">Notifications</h1><div className="mt-7 divide-y divide-line border-y border-line">{items.map(({ icon: Icon, title, body, time }, index) => <article key={title} className={`flex gap-3 py-4 ${index === 0 ? "bg-primary-soft/50" : ""}`}><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-surface-strong text-primary"><Icon size={18} /></span><div className="flex-1"><h2 className="text-sm font-semibold">{title}</h2><p className="mt-1 text-sm leading-5 text-muted">{body}</p></div><time className="score text-xs text-muted">{time}</time></article>)}</div></div>; }
