import Link from "next/link";
import { ArrowRight, CalendarPlus, ChevronRight, MapPin, Users } from "lucide-react";
import { ButtonLink } from "@/components/ui/button";
import { requireUser } from "@/features/auth/session";
import { ensureProfile } from "@/features/players/profile";
import { formatSessionDate, formatSessionTime, peso } from "@/features/sessions/format";
import { getHomeSessions } from "@/features/sessions/queries";

export default async function HomePage() {
  const user = await requireUser();
  const [profile, data] = await Promise.all([ensureProfile(user), getHomeSessions(user.id)]);
  const next = data.upcoming[0];
  return <div className="space-y-10 sm:space-y-12">
    <section className="flex items-end justify-between gap-4"><div><p className="mb-1 text-sm text-muted">Good to see you, {profile.name.split(" ")[0]}</p><h1 className="app-title">{next ? "Your next game is set." : "Ready for your next game?"}</h1></div><span className="hidden sm:block"><ButtonLink href="/games/new"><CalendarPlus size={18} />Create game</ButtonLink></span></section>

    {next ? <section aria-labelledby="next-game-heading"><h2 id="next-game-heading" className="mb-3 text-sm font-semibold text-muted">Next game</h2><article className="overflow-hidden rounded-2xl bg-court text-white ring-1 ring-black/5"><div className="grid md:grid-cols-[1fr_260px]"><div className="p-5 sm:p-7"><div className="mb-7 flex items-center justify-between"><span className="sport-label text-white/65">{formatSessionDate(next.session.startsAt)}</span><span className="inline-flex items-center gap-2 text-sm font-[650] text-court-line"><span className={`h-2 w-2 rounded-full ${next.session.bookedAt ? "bg-signal" : "bg-white/35"}`} />{next.session.bookedAt ? "Court confirmed" : "Booking pending"}</span></div><h3 className="max-w-xl text-[28px] font-[720] tracking-[-0.025em] sm:text-4xl">{next.session.title}</h3><p className="mt-2 text-base text-white/70">{next.session.venueName} · {formatSessionTime(next.session.startsAt, next.session.endsAt)}</p><div className="mt-7 flex flex-wrap items-center gap-x-5 gap-y-3 court-rule border-t pt-5"><span className="inline-flex items-center gap-2 text-sm text-white/75"><Users size={16} />{next.playerCount} / {next.session.capacity} players</span>{next.session.estimatedCostCents ? <span className="score text-sm font-semibold">{peso(next.session.estimatedCostCents)} estimated</span> : null}</div></div><Link href={`/games/${next.session.id}`} prefetch={false} className="pressable group flex min-h-20 items-center justify-between bg-primary px-5 py-4 font-semibold hover:bg-primary-hover md:flex-col md:items-start md:justify-end md:p-6"><span><span className="block text-sm text-white/75">Everything in one place</span><span className="mt-1 block text-lg">View game</span></span><ArrowRight className="transition-transform group-hover:translate-x-1" /></Link></div></article></section> : <section className="overflow-hidden rounded-2xl border border-line bg-surface lg:grid lg:grid-cols-[1.05fr_.95fr]"><div className="p-6 sm:p-8 lg:flex lg:flex-col lg:justify-between"><div><CalendarPlus className="text-primary" /><h2 className="mt-5 text-2xl font-[700] tracking-[-0.02em]">Your first game starts here</h2><p className="mt-3 max-w-md leading-7 text-muted">Set the plan once. Friends get one clear invite, and the same session carries everyone through the night.</p></div><ButtonLink href="/games/new" className="mt-7 self-start">Create your first game</ButtonLink></div><div className="court-panel relative bg-court p-6 text-white sm:p-8"><div className="relative z-10"><p className="text-sm font-[650] text-court-line">One session, start to finish</p><ol className="mt-5 divide-y divide-white/12 border-y border-white/12"><li className="grid grid-cols-[86px_1fr] gap-3 py-4"><span className="font-[650]">Before</span><span className="text-sm leading-5 text-white/65">Share the public invite and split costs.</span></li><li className="grid grid-cols-[86px_1fr] gap-3 py-4"><span className="font-[650]">Courtside</span><span className="text-sm leading-5 text-white/65">Run the paddle stack, teams, and points.</span></li><li className="grid grid-cols-[86px_1fr] gap-3 py-4"><span className="font-[650]">After</span><span className="text-sm leading-5 text-white/65">Keep results, photos, and play again.</span></li></ol></div></div></section>}

    {data.upcoming.length > 1 ? <section aria-labelledby="upcoming-heading"><div className="mb-3 flex items-center justify-between"><h2 id="upcoming-heading" className="text-lg font-bold">Upcoming games</h2><Link href="/games" className="text-sm font-semibold text-primary">View all</Link></div><div className="divide-y divide-line border-y border-line">{data.upcoming.slice(1, 4).map(({ session, playerCount }) => <Link href={`/games/${session.id}`} prefetch={false} key={session.id} className="pressable group flex items-center gap-4 py-4 hover:bg-surface sm:px-2"><time className="score grid h-14 w-14 shrink-0 place-items-center rounded-[10px] bg-surface text-center text-xs font-bold">{formatSessionDate(session.startsAt)}</time><div className="min-w-0 flex-1"><h3 className="truncate font-semibold">{session.title}</h3><p className="mt-1 truncate text-sm text-muted">{formatSessionTime(session.startsAt, session.endsAt)} · {session.venueName}</p></div><span className="score hidden text-sm sm:block">{playerCount} / {session.capacity}</span><ChevronRight className="text-muted" size={19} /></Link>)}</div></section> : null}

    {data.recent.length ? <section aria-labelledby="recent-heading"><h2 id="recent-heading" className="text-lg font-bold">Recent games</h2><div className="mt-3 divide-y divide-line border-y border-line">{data.recent.slice(0, 4).map(({ session, playerCount }) => <Link href={`/s/${session.slug}`} prefetch={false} key={session.id} className="flex min-h-20 items-center gap-4 py-4"><MapPin className="text-muted" size={19} /><div className="flex-1"><h3 className="font-semibold">{session.title}</h3><p className="mt-1 text-sm text-muted">{formatSessionDate(session.startsAt)} · {session.venueName} · {playerCount} players</p></div><ChevronRight className="text-muted" size={19} /></Link>)}</div></section> : null}
  </div>;
}
